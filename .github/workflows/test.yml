name: test

on:
  push:
    branches:
      - main
  pull_request:

env:
  FOUNDRY_PROFILE: ci

jobs:
  foundry:
    strategy:
      fail-fast: true

    name: Simulate solutions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@8998d1bcb5bb77567257de4205516c9a0cc8e973
        with:
          version: nightly-307576dfb9e94536177e940c2710045ada69d8d7 

      - name: Run Forge Scripts
        run:  sh run_all_solutions.sh
        id: run-solutions
        env:
          FORK_URL: ${{ secrets.FORK_URL }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY}}

